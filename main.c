// includes the standard input-output header
#include <stdio.h>

// entry point for C and C++ programs
int main() {

  // prints a string of characters into the screen, in this case "Hello world!"
  printf("Hello world!\n")

  // terminates a program successfully
  return 0;
}
